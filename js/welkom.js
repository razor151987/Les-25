        $(document).ready(function() {

        $("#knop").click(function() {
            
        var naam = $("#idnaam").val();    
        window.alert(
        "Uw naam is dus: " + naam + "?"
        );
         

            timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Ben je blij dat je op mijn knopje hebt geklikt, " + naam + "?"
        );

        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Nu zou ik je de hele dag bezig kunnen houden..."
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "In theorie dan, wel te verstaan..."
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Want een simpele alt-F4 gaat je helpen, of toch niet?"
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Wil je weten waarom ik voor deze opleiding heb gekozen? Ik ben Michel overigens."
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Heb je even de tijd om mijn verhaal aan te horen?"
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Niet?"
        );

        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Maar waarom dan niet? We kunnen zoveel lol hebben."
        );

        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Zal ik dan maar stoppen met berichtjes?"
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Tot de volgende keer " + naam
        );


        timeout = setTimeout('timeout_trigger()', 5000);
            clearTimeout(timeout);
            window.alert(
        "Joh, je bent er nog, " + naam + "?"
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Dan vindt je het toch wel gezellig met mij?"
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Ik loop even naar de koelkast voor een glas melk, moment.. Druk even op ok als je wilt."
        );


        timeout = setTimeout('timeout_trigger()', 10000);
            clearTimeout(timeout);
            window.alert(
        "Ik ben er weer."
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Zullen we een spelletje spelen?"
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Ook al niet, " + naam + "?"
        );


        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Nou tot de volgende keer dan maar, " + naam
        );

        timeout = setTimeout('timeout_trigger()', 1000);
            clearTimeout(timeout);
            window.alert(
        "Bedankt voor het nakijken!"
        );

    
    });
        });
